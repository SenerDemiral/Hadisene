<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    @* <link rel="stylesheet" href="bootstrap/bootstrap.min.css" /> *@
    <link rel="stylesheet" href="app.css?version=5" />
    <link rel="stylesheet" href="appOO.css?version=5" />
    <link rel="stylesheet" href="appOE.css?version=5" />
    <link rel="stylesheet" href="appOZ.css?version=5" />
    <link rel="stylesheet" href="Hadisene.styles.css?version=5" />
    <link rel="icon" type="image/png" href="favicon.png?version=1" />
    @* <link rel="preconnect" href="https://fonts.googleapis.com"> *@
    @* <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> *@
    @* <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet"> *@
    @* <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" /> *@
    @* <link rel="stylesheet" href="GoogleFonts.woff2" /> *@
    @* <HeadOutlet /> *@
</head>

<body>
    <Routes  />
    <script src="_framework/blazor.web.js"></script>
    <script src="js/deneme.js?version=2"></script>
</body>

</html>

@code{
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;
    // @rendermode="@PageRenderMode"
    //@rendermode="new InteractiveServerRenderMode(prerender: false)"
    // private IComponentRenderMode? PageRenderMode
    // => HttpContext.AcceptsInteractiveRouting() ? InteractiveServer : null;


    // css / js değişende version 1 artır ki eskisni cache den almasın
}